<?php if (!defined("PICNIC")) { header("Location: /"); exit(1); }

// This config file can be used to define some of the settings, this is mainly to make it easier to setup
// nzbVR inside a sub-directory. 

// Edit and un-comment the options and then save the file as 'config.php'

// The settings defined here will be used as the defaults and to override the current settings. If you
// want to use the settings defined from the web ui you need to comment out the option in this file.

// No mod-rewrite? Set the 'base_url' to 'index.php/'
// Sub-directory? Set the 'base_url' to '/url/to/nzbvr/'

// $c['base_url'] = "/";

?>